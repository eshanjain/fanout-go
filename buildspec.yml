version: 0.2

batch:
  fast-fail: false
  build-fanout:
    parallelism: 3
    ignore-failure: false

phases:
  install:
    commands:
      - echo 'Installing Go dependencies'
      - go version
  pre_build:
    commands:
      - echo 'prebuild'
  build:
    commands:
      - echo 'Running go Tests'
      - codebuild-tests-run --test-command "go test -v calculator.go" --files-search "echo 'add_subtract_test.go\r\ndivide_test.go\r\nmultiply_test.go'"
  post_build:
    commands:
      - echo "Would deploy here"
